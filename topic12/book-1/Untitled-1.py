def calculatePixel:
    i=math.rand()
    if i<.5
        return [0,0,0]
    else    
        return [0,255,0]

display = [calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),
    calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),
    calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),
    calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),calculatePixel(),]