# React Basics

This lab guides you through the basic concepts of React. You will complete a series of exercises to produce simple UI components.

**Tooling note:** The lab uses **Vite** (React + TypeScript) and a small in-app “lab runner” dropdown to preview components (instead of Storybook).
